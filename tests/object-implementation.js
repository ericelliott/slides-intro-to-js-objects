/**
 * Instructions:
 * 
 * Create an object literal that could be used by the user interface of a TV
 * remote control application. It should have the following properties:
 * 
 * {boolean} powerState
 * {number}  channel
 * {number}  volume          0 - 100
 * 
 * And the following methods:
 *
 * togglePower({boolean})
 * 
 * setChannel({number})
 * 
 * setVolume({number})
 */
